apply plugin: 'com.android.library'

dependencies {
}

android {
    signingConfigs {
        rilis {
            storeFile file('C:\\Users\\FANDO\\test.jks')
            storePassword 'Palang66'
            keyAlias = 'test'
            keyPassword 'Palang66'
        }
    }
    compileSdkVersion 27
    defaultConfig {
        minSdkVersion 17
        targetSdkVersion 27
    }
    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            jniLibs.srcDirs = ['libs']
            aidl.srcDirs = ['src']
            renderscript.srcDirs = ['src']
            res.srcDirs = ['res']
        }

    }
    buildTypes {
        release {
            signingConfig signingConfigs.rilis
        }
    }

}
